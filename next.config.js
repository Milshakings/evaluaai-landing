// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aumenta el timeout a 300 segundos (por defecto es 60)
  staticPageGenerationTimeout: 300,
};

module.exports = nextConfig;
