// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aumenta el timeout a 120 segundos (por defecto es 60)
  staticPageGenerationTimeout: 120,
};

module.exports = nextConfig;
